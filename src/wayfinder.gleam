import filepath
import gleam/list
import gleam/string
import justin
import wayfinder/lib

pub fn generate(routes: List(lib.RouteWrapper)) {
  todo
  // TODO find invalid paths -> too few parameters
  // TODO find invalid paths -> missing parameter name
}
